apiVersion: v1
kind: ConfigMap
metadata:
  name: olake-workers-config
  namespace: {{ include "olake.namespace" . }}
  annotations: {}
  labels:
    {{- include "olake.labels" . | nindent 4 }}
    app.kubernetes.io/component: workers
data:
  # Temporal Configuration
  {{- if .Values.temporal.enabled }}
  TEMPORAL_ADDRESS: "temporal.{{ include "olake.namespace" . }}.svc.cluster.local:7233"
  {{- end }}
  
  # Kubernetes Configuration
  WORKER_NAMESPACE: {{ include "olake.namespace" . | quote }}
  OLAKE_STORAGE_PVC_NAME: {{ include "olake.sharedStoragePVC" . | quote }}

  # Logging Configuration
  LOG_LEVEL: {{ .Values.olakeWorker.config.logging.level | quote }}
  LOG_FORMAT: {{ .Values.olakeWorker.config.logging.format | quote }}
  
  # Runtime Configuration
  RUN_MODE: {{ .Values.olakeWorker.env.RUN_MODE | default .Values.global.env.RUN_MODE | default "staging" }}

  # API Callback
  OLAKE_CALLBACK_URL: "http://olake-ui.{{ include "olake.namespace" . }}.svc.cluster.local:8000/internal/worker/callback"
  # =================================================================
  # JOB MAPPING CONFIGURATION (Deprecated)
  # =================================================================
  {{- if .Values.global.jobMapping }}
  OLAKE_JOB_MAPPING: {{ .Values.global.jobMapping | toJson | quote }}
  {{- end }}

  # =================================================================
  # JOB PROFILES CONFIGURATION
  # =================================================================
  # A profile carries pod scheduling only (nodeSelector / tolerations / affinity).
  # Profile 0 is the default every job inherits; a JobID profile is deep-merged
  # over it field by field by the worker. Index volumes are configured separately,
  # in the top-level `jobIndexes` block below.
  {{- if .Values.global.jobProfiles }}
  # Converted to JSON for consistent parsing by the worker
  OLAKE_JOB_PROFILES: {{ .Values.global.jobProfiles | toJson | quote }}
  {{- end }}

  # =================================================================
  # JOB INDEX STORAGE CONFIGURATION
  # =================================================================
  # `default` holds the index settings every job starts from; entries under
  # `jobs` are deep-merged over it field by field by the worker. Always rendered
  # so `global.storageClass` reaches the index even when the block is empty. A
  # job only gets a volume when its `index_required` advanced setting is true,
  # whatever is configured here.
  {{- $indexes := deepCopy (.Values.jobIndexes | default dict) }}
  {{- $indexDefault := deepCopy (get $indexes "default" | default dict) }}
  {{- if not (get $indexDefault "storageClass") }}
  {{- $_ := set $indexDefault "storageClass" (.Values.global.storageClass | default "") }}
  {{- end }}
  {{- $_ := set $indexes "default" $indexDefault }}
  OLAKE_JOB_INDEXES: {{ $indexes | toJson | quote }}

  # =================================================================
  # JOB SERVICE ACCOUNT CONFIGURATION
  # =================================================================
  JOB_SERVICE_ACCOUNT_NAME: {{ include "olake.jobServiceAccountName" . | quote }}


  # =================================================================
  # POD SECURITY CONTEXT CONFIGURATION
  # =================================================================
  {{- if .Values.olakeWorker.securityContext }}
  POD_SECURITY_CONTEXT: {{ .Values.olakeWorker.securityContext | toJson | quote }}
  {{- end }}

  # =================================================================
  # ACTIVITY POD ANNOTATIONS CONFIGURATION
  # =================================================================
  {{- if .Values.global.podAnnotations }}
  OLAKE_JOB_POD_ANNOTATIONS: {{ .Values.global.podAnnotations | toJson | quote }}
  {{- end }}